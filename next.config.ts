import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactCompiler: true,
  // the dev-tools badge sits exactly where the demo pill lives (bottom-left)
  devIndicators: false,
};

export default nextConfig;
